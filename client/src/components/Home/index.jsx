import React, { useEffect, useState } from 'react';
import Navbar from '../NavBar';
import Cards from '../Cards';

const Home = () => {
    return (
        <div>
            <Navbar />
            <Cards />
        </div>
    );
};

export default Home;
